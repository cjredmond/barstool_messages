
import { formatTimezone } from 'barstool-messages/helpers/format-timezone';
import { module, test } from 'qunit';

module('Unit | Helper | format timezone');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatTimezone([42]);
  assert.ok(result);
});

