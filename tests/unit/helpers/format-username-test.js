
import { formatUsername } from 'barstool-messages/helpers/format-username';
import { module, test } from 'qunit';

module('Unit | Helper | format username');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatUsername([42]);
  assert.ok(result);
});

